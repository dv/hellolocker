.container
  - if @items.any?
    .row
      List of items

    - @items.each do |item|
      .row
        %h2
          = item.url
        %ul
          - item.links.each do |link|
            %li
              = link_to label_url(link) do
                = label_url(link)

          %li
            = link_to new_item_link_path(item) do
              Add New
  - else
    .row
      No items available
